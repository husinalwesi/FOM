import { onlyNumbersDirective } from './only-numberes.directive';

describe('onlyNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new onlyNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
