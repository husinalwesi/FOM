import { VideoSourcePipe } from './video-source.pipe';

describe('VideoSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
