import { FormatDateToApiFormatPipe } from './format-date-to-api-format.pipe';

describe('FormatDateToApiFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateToApiFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
