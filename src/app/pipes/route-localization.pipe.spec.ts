import { RouteLocalizationPipe } from './route-localization.pipe';

describe('RouteLocalizationPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteLocalizationPipe();
    expect(pipe).toBeTruthy();
  });
});
